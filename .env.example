# HTTP port for Express
PORT=3000
# WebSocket path
WS_PATH=/ws
# Token required for POST /owntracks; set the same token in OwnTracks
OWNTRACKS_TOKEN=change-me
# Optional: Nominatim contact email (recommended for polite usage)
NOMINATIM_EMAIL=hugo.combe.84@gmail.com
# Optional: full User-Agent string for Nominatim
# NOMINATIM_USER_AGENT=gps-owntracks-ws-server/1.0 (you@example.com)
# Preferred language for Nominatim responses
NOMINATIM_LANGUAGE=fr
# Set to false to disable reverse geocoding
REVERSE_GEOCODE=true
